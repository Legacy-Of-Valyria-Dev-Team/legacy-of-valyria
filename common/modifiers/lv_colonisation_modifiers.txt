# Override from CK3/AGOT (this was missing an icon)
extra_building_slot = {
	icon = county_modifier_development_positive
	stacking = yes
	building_slot_add = 1
}

# Wasteland modifiers
wasteland_valyria = {
	icon = beast_mixed
	travel_danger = 100

	scale = {
		value = {
			value = 0
			if = {
				limit = { any_county_province = { lv_is_fourteen_flames = yes } }
				value = 1
			}
			else_if = {
				limit = { any_neighboring_county = { any_county_province = { lv_is_fourteen_flames = yes } } }
				value = 0.75
			}
			else_if = {
				limit = { any_neighboring_county = { any_neighboring_county = { any_county_province = { lv_is_fourteen_flames = yes } } } }
				value = 0.5
			}
			else = { value = 0.25 }
			multiply = {
				value = 1
				multiply = {
					if = {
						limit = { has_variable = lv_blocker_count }
						value = var:lv_blocker_count
					}
					else = { value = 1 }
				}
				divide = {
					if = {
						limit = { has_variable = lv_blocker_total }
						value = var:lv_blocker_total
					}
					else = { value = 1 }
				}
			}
			desc = wasteland_valyria_proximity_to_volcano
		}
		desc = wasteland_valyria_epicenter
	}
}