activity_colonisation = {

	is_shown = {
		is_ai = no
		OR = {
			has_dynasty_modifier = valyrian_heritage_dynasty_modifier
			has_trait = zz_magister 
        	has_trait = dragon_dreams
			AND = {
				is_landed = no
				has_trait = lifestyle_traveler
				has_trait_xp = {
					trait = lifestyle_traveler
					track = travel
					value >= 100
				}
				OR = {
					has_trait = ambitious
					has_trait = brave
				}
			}
		}
	}
	
	is_valid = {
		is_landed = yes
		is_at_war = no
		is_imprisoned = no
	}
	
	cost = { gold = 800 }
	
	on_host_death = {
	}

	is_location_valid = {
		county_region = valyria
	}
	host_description = {
		desc = activity_colonisation_host_desc
	}
	is_grand_activity = no
	is_signle_location = yes
	province_filter = { county_region = valyria }




}