Mask = {
    physical = yes

    intrigue = 5
    
	ruler_designer_cost = 100

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					NOT = { exists = this }
				}
				desc = trait_mask_desc
			}
			desc = trait_mask_character_desc
		}
	}

    icon = mask.dds
}
