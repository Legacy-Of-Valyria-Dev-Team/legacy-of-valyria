namespace = blockers

blockers.0002 = {	#Clearing a mine of the fourteen flames for the first time
	type = character_event
	title = blockers.0002.t
	desc = blockers.0002.desc
	theme = realm

	right_portrait = {
		character = root
		animation = happiness
	}

	artifact = {
		target = scope:epic
		position = lower_left_portrait
	}
	override_background = {
		reference = agot_dragonstone_cave
	}

	immediate = {
		hidden_effect = {
			agot_create_random_dragon_egg_artifact = {
				OWNER = root
			}
		}
	}
	option = {
		name = blockers.0002.a
	}
}

blockers.0003 = {	#Upgrading RedWyrm's Wrath
	type = character_event
	title = blockers.0003.t
	desc = blockers.0003.desc
	theme = realm

	right_portrait = {
		character = root
		animation = personality_rational
	}

	artifact = {
		target = scope:newly_created_artifact
		position = lower_left_portrait
	}
	override_background = {
		reference = lov_vault_01
	}

	immediate = {
		hidden_effect = {
			lv_create_artifact_vs_torment_effect = {
				OWNER = root
			}
		}
	}

	option = {
		name = blockers.0003.a
		custom_tooltip = gain_vs_artifact
		add_gold = 250
		add_prestige = 1000
		dynasty = {
			add_dynasty_prestige = 500
		}
	}
}

blockers.0004 = {	#Upgrading the Tower of Lyria
	type = character_event
	title = blockers.0004.t
	desc = blockers.0004.desc
	theme = realm

	right_portrait = {
		character = root
		animation = personality_rational
	}

	artifact = {
		target = scope:newly_created_artifact
		position = lower_left_portrait
	}
	# override_background = {
	# 	reference = agot_dragonstone_cave
	# }

	immediate = {
		hidden_effect = {
			lv_create_artifact_vs_mages_fire_effect = {
				OWNER = root
			}
		}
	}

	option = {
		name = blockers.0004.a
		custom_tooltip = gain_vs_artifact
		add_prestige = 1000
		dynasty = {
			add_dynasty_prestige = 500
		}
	}
}

blockers.0005 = {	#Clearing a major ruin
	type = character_event
	title = blockers.0005.t
	desc = blockers.0005.desc
	theme = realm

	right_portrait = {
		character = root
		animation = happiness
	}

	artifact = {
		target = scope:epic
		position = lower_left_portrait
	}
	override_background = {
		reference = AGOT_ruins_inside
	}

	immediate = {
		hidden_effect = {
			agot_create_random_dragon_egg_artifact = {
				OWNER = root
			}
		}
	}
	option = {
		name = blockers.0005.a
	}
}
