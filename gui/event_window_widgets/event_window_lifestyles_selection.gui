widget = {
	allow_outside = yes
	name = "event_window_county_selection"
	size = { 450 250 }

	vbox = {
		#text_single = {block "header" { text = "This is a Header" } }
		container = {
			allow_outside = yes
			layoutpolicy_horizontal = expanding
			layoutpolicy_vertical = expanding
			datacontext = "[GetPlayer.MakeScope]"
			vbox = {
				allow_outside = yes

				scrollbox = {
					layoutpolicy_horizontal = expanding
					layoutpolicy_vertical = expanding
					minimumsize = { 450 180 }
					blockoverride "scrollbox_content" {
						vbox = {
							datamodel = "[Scope.GetList('possible_counties')]"

							item = {
								button_standard = {
									size = { 410 45 }
									datacontext = "[Scope.County]"

									down = "[GetScriptedGui( 'zz_valyria_county_selecting' ).IsShown( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_county', County.MakeScope ).End )]"
									onclick = "[GetScriptedGui( 'zz_valyria_county_selecting' ).Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_county', County.MakeScope ).End )]"

									widget = {
										size = { 360 45 }
										parentanchor = right
										text_single = {
											text = "[County.GetName]"
											parentanchor = center
											#default_format = "#high"
											using = Font_Size_Medium
										}
									}
								}
							}
						}
						spacer = { size = { 0 20 } }
					}
				}
			}
		}
	}
}